const BASE_URL = process.env.BASE_URL;
const CMS_TOKEN = process.env.CMS_TOKEN;
const CMS_URL = process.env.CMS_URL;

export const API = {
  ARTICLE: {
    GET_ALL_ARTICLE: `${process.env.CMS_URL}/article`,
  },
};
